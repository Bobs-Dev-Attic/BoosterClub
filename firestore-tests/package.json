{
  "name": "firestore-tests",
  "private": true,
  "description": "Firestore & Storage security-rules tests (run against the emulator).",
  "scripts": {
    "test": "firebase emulators:exec --only firestore,storage --project demo-booster \"node --test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0",
    "firebase-tools": "^13.29.1"
  }
}
